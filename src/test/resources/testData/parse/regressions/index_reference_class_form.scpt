-- Literal `index` keyword form of the index reference class form (osacompile-valid).
-- Guards indexReferenceClassForm against regression from the soft-keyword identifier seam.
set x to item index 1 of {10, 20, 30}
