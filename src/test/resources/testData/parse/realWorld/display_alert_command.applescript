(*
    PARSE: multi-word Standard Additions command (`display alert`) with labeled parameters,
    parsed via the fallback command path with no loaded dictionary.
*)
display alert "Title" message "Body" buttons {"Cancel"}
